{
  "name": "edraky-test",
  "version": "0.1.0",
  "dependencies": {
    "@sap/cds": "^9"
  },
  "devDependencies": {
    "@cap-js/cds-typer": ">=0.1",
    "@cap-js/cds-types": "^0.16.0",
    "@cap-js/sqlite": "^2",
    "@types/node": "^24.0.0",
    "tsx": "^4",
    "typescript": "^5",
    "vitest": "^4.1.1"
  },
  "scripts": {
    "start": "cds-serve",
    "test": "vitest run"
  },
  "private": true,
  "imports": {
    "#cds-models/*": "./@cds-models/*/index.js"
  },
  "cds": {
    "requires": {
      "auth": "mocked"
    }
  }
}
